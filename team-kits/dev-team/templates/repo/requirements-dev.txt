# Dev/quality + security tooling the pipeline (scripts/quality.py + the merge gate) needs for a Python
# stack. DevOps installs these at project start: pip install -r requirements-dev.txt
ruff
mypy
pytest
pytest-cov
pip-audit          # SCA (dependency vulnerabilities)
bandit             # SAST (static security analysis)
cyclonedx-bom      # SBOM generation
# Secret scanning uses gitleaks (a standalone binary, installed in CI via its action, not pip).
