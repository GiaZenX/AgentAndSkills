# research_questions.yaml — owned by: PM
# The customer-visible research goals. The user NEVER writes these directly.
# Status: PROPOSED -> APPROVED -> INVESTIGATED -> VALIDATED -> ACCEPTED / REJECTED
#
# Schema:
#   RQ-XXXX:
#     title: <short research goal>
#     description: <plain language>
#     acceptance_criteria: [ ... ]      # what makes the answer acceptable
#     status: PROPOSED
#     complete: false                   # true only when discovery is finished
#     branch: feat/RQ-XXXX-...          # set in SYSTEM_PLANNING
#     created: YYYY-MM-DD               # start date, set when first written (PROPOSED)
#     closed: ""                        # end date, set when ACCEPTED / REJECTED

research_questions: {}
