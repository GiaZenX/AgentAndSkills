# Hours log — FZulG Eigenleistung proof

Running, per-session proof of personnel hours for the Forschungszulage (BSFZ) application. **The applicant
maintains this by hand — the AI never fills hours here.** Each row: date, hours, work package (AP nr from
`project_memory/fzulg_documentation.yaml`), and the concrete experimental activity. The totals must match the
`effort` block in `fzulg_documentation.yaml`.

Rules (FZulG): max 40 h/week; eligible Eigenleistung is capped per § 3 Abs. 3 FZulG (e.g. 100 €/h, up to the
statutory weekly maximum); only work from the project start date onward is eligible (prior exploratory work
is not).

| Date (YYYY-MM-DD) | Hours | AP (Nr) | Activity (concrete experimental work) |
|---|---|---|---|
|  |  |  |  |

**Running total:** 0 h
